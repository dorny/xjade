/*
    some source
*/

var anonymous = function (parent) {
  var __expr;
  if (!parent) parent=document.createDocumentFragment();
  return parent;
}

// som code

function named(parent) {
  var __expr;
  if (!parent) parent=document.createDocumentFragment();
  return parent;
}

// some code
